language: node_js
node_js:
  - "node"
cache:
  directories:
    - "node_modules"
before_deploy: npm install now --no-save 
install:
  - npm run build
deploy:
  - provider: script 
    script: 
      - npm run deploy